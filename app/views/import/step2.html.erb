<%- title 'Userscripts.org import step 2' %>

<p>userscripts.org account verified. Choose the scripts you would like to import.</p>

<%=form_tag(import_step3_path) do |f|%>

	<h2>New scripts</h2>
	<% if @new_scripts.empty? %>
		<p>(None)</p>
	<% else %>
		<ul>
			<% @new_scripts.each do |id, name| %>
				<li>
					<input id="userscripts-<%=id%>" type="checkbox" name="userscripts_ids[]" value="<%=id%>"><label for="userscripts-<%=id%>"><%=name%></label>
					<input type="hidden" name="imported-name-<%=id%>" value="<%=name%>">
				</li>
			<% end %>
		</ul>
	<% end %>

	<h2>Previously imported scripts</h2>
	<% if @updated_scripts.empty? %>
		<p>(None)</p>
	<% else %>
		<p>The entries for these scripts on Greasy Fork will be updated to the current code on userscripts.org.</p>
		<ul>
			<% @updated_scripts.each do |id, name| %>
				<li>
					<input id="userscripts-<%=id%>" type="checkbox" name="userscripts_ids[]" value="<%=id%>"><label for="userscripts-<%=id%>"><%=name%></label>
					<input type="hidden" name="imported-name-<%=id%>" value="<%=name%>">
				</li>
			<% end %>
		</ul>
	<% end %>

	<button class="select-all">Select all</button> <button class="select-none">Select none</button> <%=submit_tag 'Import'%>
<% end %>
